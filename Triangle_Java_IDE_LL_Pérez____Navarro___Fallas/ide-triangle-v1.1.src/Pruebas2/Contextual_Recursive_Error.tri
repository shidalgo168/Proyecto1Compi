!"recursive" Proc-Funcs "end"

let
  recursive
      func F () : Integer ~ 10
  and
      proc P (x : Integer) ~
        putint (x * 2) end
  end;
  func T() : Integer ~ F()    ! la func T esta fuera del recursive, por lo que esta afuera del scope
in
  P (F())
end