!"local" Declaration "in" Declaration "end"
! LOCAL DECLARACION dentro de un let


let
    local          	
      var x : Integer				! Declaration
    in
      proc P (y : Integer) ~ putint (x * y) end	! Declaration
    end
in
    P(5);
    putint(x) ! la variable x es visible solo dentro de la declaracion local
end 